import styled from 'styled-components';

// configs
import colors from '../../configs/colors';

const TitleSubheader = styled.h2`
  font-size: 1.5rem;
  margin: 0;
  color: ${colors.purple};
  text-transform: lowercase;
`

export default TitleSubheader;